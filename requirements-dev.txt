-r requirements.txt

# Testing (pytest + FastAPI TestClient via httpx/starlette)
pytest>=7.4
pytest-asyncio>=0.23
pytest-cov>=4.1
pytest-mock>=3.14
httpx>=0.27

# Lint / format
ruff>=0.4

# Time-based tests (commissions, follow-ups, approval windows)
freezegun>=1.4
