[pytest]
testpaths = tests
pythonpath = .
markers =
    allow_real_twilio_send: run send_sms without the default autouse mock
    no_partner_tax_encryption_key: skip autouse partner tax encryption key fixture
